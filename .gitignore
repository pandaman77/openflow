# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage

# Node
node_modules/
dist/
dist-ssr/

# Rust
target/

# Models (веса не храним в git — только манифесты)
models/*.bin
models/*.gguf
models/*.onnx
models/whisper-*/
models/llm/

# IDE / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# Local
*.log
tmp/
.env

# PyInstaller build artifacts
build/
dist/
*.spec

# CI added separately (needs workflow-scope token)
.github/workflows/

# but the handwritten sidecar spec is a build source, keep it
!openflow-engine-x86_64-pc-windows-msvc.spec
